name: CI/CD Pipeline Complète avec SonarQube

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-check:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Temps augmenté pour SonarQube

    services:
      sonarqube:
        image: sonarqube:community
        ports: [9000:9000]
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
        options: --memory 4g --cpus 2

    steps:
      # Étape 1: Récupération du code
      - name: 🛎️ Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Nécessaire pour SonarQube

      # Étape 2: Installation des dépendances
      - name: 📦 Installer les dépendances
        run: npm install

      # Étape 3: Exécution des tests avec couverture
      - name: 🧪 Exécuter les tests
        run: npm test -- --coverage

      # Étape 4: Analyse SonarQube (version corrigée)
      - name: 🔍 Analyse SonarQube
        env:
          SONAR_HOST_URL: "http://localhost:9000"  # Correction ici
        run: |
          # Vérification améliorée du démarrage
          echo "Vérification du démarrage de SonarQube..."
          for i in {1..30}; do
            if nc -z localhost 9000 && \
               curl -sSf "$SONAR_HOST_URL/api/system/status" | grep -q '"status":"UP"'; then
              echo "SonarQube prêt après $((i*3)) secondes"
              break
            fi
            sleep 3
            if [ $i -eq 30 ]; then
              echo "::error::SonarQube non démarré après 90s"
              echo "::group::Logs SonarQube"
              docker logs $(docker ps -aqf "name=sonarqube")
              echo "::endgroup::"
              exit 1
            fi
          done

          # Installation garantie du scanner
          npm install -g sonarqube-scanner

          # Analyse avec paramètres complets
          sonar-scanner \
            -Dsonar.projectKey=biblio-gatekeeper \
            -Dsonar.projectName=BiblioGatekeeper \
            -Dsonar.sources=src \
            -Dsonar.tests=test \
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info \
            -Dsonar.login=admin \
            -Dsonar.host.url=$SONAR_HOST_URL

      # Étape 5: Construction de l'image Docker
      - name: 🐳 Build Docker Image
        run: docker build -t touatifadwa/biblio-gatekeeper:latest .

      # Étape 6: Push vers Docker Hub
      - name: ⬆️ Push Docker Image
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
          docker push touatifadwa/biblio-gatekeeper:latest

      # Étape 7: Déploiement Kubernetes
      - name: 🚀 Deploy to Kubernetes
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          echo "$KUBE_CONFIG" > kubeconfig.yaml
          export KUBECONFIG=kubeconfig.yaml
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml

      # Étape 8: Vérification des pods
      - name: ✅ Check Pod Status
        run: |
          kubectl wait --for=condition=ready pod -l app=biblio-gatekeeper --timeout=120s
          kubectl get pods -o wide

      # Étape 9: Notification
      - name: 📨 Send Notification
        if: success()
        run: |
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Déploiement réussi ✅ - Rapport SonarQube: http://localhost:9000/dashboard?id=biblio-gatekeeper"

      # Étape 10: Rollback automatique
      - name: 🔄 Rollback on Failure
        if: failure()
        run: |
          kubectl rollout undo deployment/biblio-gatekeeper
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Échec du déploiement ❌ - Rollback initié"