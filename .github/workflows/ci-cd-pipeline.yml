name: CI/CD Pipeline Complète avec SonarQube

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-check:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    services:
      sonarqube:
        image: sonarqube:community
        ports: [9000:9000]
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
          SONAR_FORCE_AUTHENTICATION: "false"
        options: >-
          --memory 4g
          --cpus 2

    steps:
      - name: 🛎️ Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Installer les dépendances
        run: npm install

      - name: 🧪 Exécuter les tests
        run: |
          npm test -- --coverage
          mv coverage/lcov.info coverage/lcov-full.info

      - name: ⚙️ Configurer SonarQube (version originale préservée)
        id: sonarqube-setup
        env:
          SONAR_HOST_URL: "http://localhost:9000"
        run: |
          # Attente optimisée du démarrage
          echo "⏳ Attente du démarrage de SonarQube..."
          for i in {1..30}; do
            if curl -sSf "$SONAR_HOST_URL/api/system/status" | grep -q '"status":"UP"'; then
              echo "✅ SonarQube prêt après $((i*2)) secondes"
              
              # Désactivation complète de l'authentification
              curl -sS -u admin:admin -X POST \
                "$SONAR_HOST_URL/api/settings/set?key=sonar.forceAuthentication&value=false"
              
              # Configuration supplémentaire pour résoudre le problème 401
              curl -sS -u admin:admin -X POST \
                "$SONAR_HOST_URL/api/permissions/add_group?groupName=Anyone&permission=scan"
              
              exit 0
            fi
            sleep 2
          done
          echo "::error::Échec du démarrage de SonarQube"
          exit 1

      - name: 🔍 Analyser avec SonarQube
        env:
          SONAR_HOST_URL: "http://localhost:9000"
        run: |
          npm install -g sonarqube-scanner
          
          # Générer un projectKey stable
          PROJECT_KEY="BIBLIO_$(echo ${GITHUB_REPOSITORY} | tr '[:lower:]' '[:upper:]' | tr -cd '[:alnum:]_')"
          echo "ℹ️ ProjectKey: $PROJECT_KEY"
          
          sonar-scanner \
            -Dsonar.projectKey="$PROJECT_KEY" \
            -Dsonar.projectName="Bibliotheque" \
            -Dsonar.host.url="$SONAR_HOST_URL" \
            -Dsonar.sources=src \
            -Dsonar.tests=tests \
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov-full.info \
            -Dsonar.sourceEncoding=UTF-8 \
            -Dsonar.working.directory=.scannerwork \
            -Dsonar.scm.disabled=true

      - name: 🐳 Construire l'image Docker
        run: docker build -t touatifadwa/biblio-gatekeeper:latest .

      - name: ⬆️ Pousser l'image Docker
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
          docker push touatifadwa/biblio-gatekeeper:latest

      - name: 🚀 Déployer sur Kubernetes
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          echo "$KUBE_CONFIG" > kubeconfig.yaml
          export KUBECONFIG=kubeconfig.yaml
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml

      - name: ✅ Vérifier l'état des pods
        run: |
          kubectl wait --for=condition=ready pod -l app=biblio-gatekeeper --timeout=120s
          kubectl get pods -o wide

      - name: 📨 Envoyer une notification
        if: success()
        run: |
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Déploiement réussi ✅ - Rapport SonarQube: $SONAR_HOST_URL/dashboard?id=$PROJECT_KEY"

      - name: 🔄 Rollback en cas d'échec
        if: failure()
        run: |
          kubectl rollout undo deployment/biblio-gatekeeper
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Échec du déploiement ❌ - Rollback initié"