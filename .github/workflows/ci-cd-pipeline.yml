name: CI/CD Pipeline Complète avec SonarQube

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-check:
    runs-on: ubuntu-latest
    timeout-minutes: 60  # Temps augmenté pour SonarQube

    services:
      sonarqube:
        image: sonarqube:community
        ports: [9000:9000]
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
          SONAR_FORCE_AUTHENTICATION: "false"  # Désactive l'authentification obligatoire
        options: >-
          --memory 4g
          --cpus 2

    steps:
      # Étape 1: Récupération du code
      - name: 🛎️ Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Nécessaire pour SonarQube

      # Étape 2: Installation des dépendances
      - name: 📦 Installer les dépendances
        run: npm install

      # Étape 3: Exécution des tests avec couverture
      - name: 🧪 Exécuter les tests
        run: npm test -- --coverage

      # Étape 4: Configuration SonarQube
      - name: ⚙️ Configurer SonarQube
        id: sonarqube-setup
        env:
          SONAR_HOST_URL: "http://localhost:9000"
        run: |
          # Attente optimisée du démarrage
          echo "⏳ Attente du démarrage de SonarQube..."
          for i in {1..30}; do
            if curl -sSf "$SONAR_HOST_URL/api/system/status" | grep -q '"status":"UP"'; then
              echo "✅ SonarQube prêt après $((i*2)) secondes"
              
              # Désactivation complète de l'authentification si nécessaire
              curl -sS -u admin:admin -X POST \
                "$SONAR_HOST_URL/api/settings/set?key=sonar.forceAuthentication&value=false"
              exit 0
            fi
            sleep 2
          done
          echo "::error::Échec du démarrage de SonarQube"
          exit 1

      # Étape 5: Analyse SonarQube (version corrigée)
      - name: 🔍 Analyser avec SonarQube
        env:
          SONAR_HOST_URL: "http://localhost:9000"
        run: |
          # Installation garantie du scanner
          npm install -g sonarqube-scanner
          
          # Paramètres dynamiques
          SONAR_PARAMS=(
            "-Dsonar.projectKey=biblio-gatekeeper"
            "-Dsonar.projectName=BiblioGatekeeper"
            "-Dsonar.host.url=$SONAR_HOST_URL"
            "-Dsonar.sources=src"
            "-Dsonar.javascript.lcov.reportPaths=coverage/lcov.info"
            "-Dsonar.scm.disabled=true"  # Désactive l'analyse SCM
          )
          
          # Ajout conditionnel de l'authentification
          if [[ -n "${{ secrets.SONAR_TOKEN }}" ]]; then
            SONAR_PARAMS+=("-Dsonar.token=${{ secrets.SONAR_TOKEN }}")
            echo "ℹ️ Utilisation du token configuré"
          else
            echo "::warning::Analyse sans authentification"
          fi
          
          # Exécution de l'analyse
          sonar-scanner "${SONAR_PARAMS[@]}"

      # Étape 6: Construction de l'image Docker
      - name: 🐳 Construire l'image Docker
        run: docker build -t touatifadwa/biblio-gatekeeper:latest .

      # Étape 7: Push vers Docker Hub (version sécurisée)
      - name: ⬆️ Pousser l'image Docker
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
          docker push touatifadwa/biblio-gatekeeper:latest

      # Étape 8: Déploiement Kubernetes
      - name: 🚀 Déployer sur Kubernetes
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          echo "$KUBE_CONFIG" > kubeconfig.yaml
          export KUBECONFIG=kubeconfig.yaml
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml

      # Étape 9: Vérification des pods
      - name: ✅ Vérifier l'état des pods
        run: |
          kubectl wait --for=condition=ready pod -l app=biblio-gatekeeper --timeout=120s
          kubectl get pods -o wide

      # Étape 10: Notification de succès
      - name: 📨 Envoyer une notification
        if: success()
        run: |
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Déploiement réussi ✅ - Rapport SonarQube: http://localhost:9000/dashboard?id=biblio-gatekeeper"

      # Étape 11: Rollback automatique
      - name: 🔄 Rollback en cas d'échec
        if: failure()
        run: |
          kubectl rollout undo deployment/biblio-gatekeeper
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Échec du déploiement ❌ - Rollback initié"