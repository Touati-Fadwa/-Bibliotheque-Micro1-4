name: CI/CD Pipeline avec SonarQube

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-assurance:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Augmenté pour laisser le temps à SonarQube de démarrer

    services:
      sonarqube:
        image: sonarqube:community
        ports: [9000:9000]
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
        options: --memory 4g --cpus 2

    steps:
      # Étape 1: Checkout du code
      - name: 🛎️ Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Étape 2: Installation des dépendances
      - name: 📦 Installer les dépendances
        run: npm install

      # Étape 3: Exécution des tests avec couverture
      - name: 🧪 Exécuter les tests
        run: npm test -- --coverage

      # Étape 4: Configuration et analyse SonarQube
      - name: 🔍 Analyse SonarQube
        env:
          SONAR_HOST_URL: "http://sonarqube:9000"
        run: |
          # Attente intelligente du démarrage de SonarQube
          echo "En attente du démarrage de SonarQube..."
          for i in {1..18}; do
            if curl -sSf "$SONAR_HOST_URL/api/system/status" | grep -q '"status":"UP"'; then
              echo "SonarQube prêt après $((i*5)) secondes"
              break
            fi
            sleep 5
            if [ $i -eq 18 ]; then
              echo "::error::Échec du démarrage de SonarQube après 90 secondes"
              exit 1
            fi
          done

          # Installation du scanner si nécessaire
          if ! command -v sonar-scanner &> /dev/null; then
            npm install -g sonarqube-scanner
          fi

          # Analyse du code
          sonar-scanner \
            -Dsonar.projectKey=biblio-gatekeeper \
            -Dsonar.projectName=BiblioGatekeeper \
            -Dsonar.sources=src \
            -Dsonar.tests=test \
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info \
            -Dsonar.login=admin

      # Étape 5: Build Docker
      - name: 🐳 Construire l'image Docker
        run: docker build -t touatifadwa/biblio-gatekeeper:latest .

      # Étape 6: Push vers Docker Hub
      - name: ⬆️ Pousser l'image Docker
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
          docker push touatifadwa/biblio-gatekeeper:latest

      # Étape 7: Déploiement Kubernetes
      - name: 🚀 Déployer sur Kubernetes
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          echo "$KUBE_CONFIG" > kubeconfig.yaml
          export KUBECONFIG=kubeconfig.yaml
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml

      # Étape 8: Vérification des pods
      - name: ✅ Vérifier l'état des pods
        run: |
          kubectl wait --for=condition=ready pod -l app=biblio-gatekeeper --timeout=120s
          kubectl get pods -o wide

      # Étape 9: Notification
      - name: 📨 Envoyer une notification
        if: success()
        run: |
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Déploiement réussi ✅ - Rapport SonarQube: http://sonarqube:9000/dashboard?id=biblio-gatekeeper"

      # Étape 10: Rollback automatique
      - name: 🔄 Rollback en cas d'échec
        if: failure()
        run: |
          kubectl rollout undo deployment/biblio-gatekeeper
          curl -X POST "https://gotify.example.com/message?token=${{ secrets.GOTIFY_TOKEN }}" \
            -d "title=CI/CD Status" \
            -d "message=Échec du déploiement ❌ - Rollback initié"