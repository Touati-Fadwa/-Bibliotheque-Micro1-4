apiVersion: v1
kind: Service
metadata:
  name: biblio-service
spec:
  selector:
    app: biblio  # ← Doit matcher le label du deployment
  ports:
    - port: 80
      targetPort: 3000  # ← Doit matcher le containerPort
  type: ClusterIP  # Pour accès interne uniquement